# 全局复习模式（"今天该复习什么"）

1. 读取 `_全局索引.md` 中的"最近复习到期（最早）"列。
2. 将到期日期 ≤ 今天的项目按紧急度分组：
   - 已过期
   - 今天到期
   - 未来 3 天即将到期
3. 如果没有任何到期项，告知用户并建议继续最近学习过的主题。
4. 每季度（距离上次长期掌握回顾 ≥ 90 天）检查是否有标记为「长期掌握」的课程（即第 5 次复习通过的课程），列出并询问用户是否需要抽查。若用户跳过，记录本次检查日期，下次 90 天后再次提示。
